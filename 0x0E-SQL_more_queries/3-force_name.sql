-- 3 first students in the Batch ID=3
-- because Batch 3 is th best!
-- that creates the table force_name on your MySQL server
CREATE TABLE IF NOT EXISTS `force_name` (
	`id` INT,
	`name` VARCHAR(256) NOT NULL);
