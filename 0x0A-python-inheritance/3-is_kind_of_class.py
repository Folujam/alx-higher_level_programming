#!/usr/bin/python3
"""
same class or inherit from
"""


def is_kind_of_class(obj, a_class):
    """
    In this code, isinstance(obj, a_class)
    checks if obj is an instance of a_class or a subclass of a_class.
    """
    return isinstance(obj, a_class)
